{
  "name": "dasrick_site_package",
  "version": "0.0.0",
  "description": "",
  "license": "MIT",
  "dependencies": {
    "dasrick-bootstrap": "^0.7.0"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-imagemin": "^4.1.0"
  },
  "scripts": {
    "postinstall": "npm run build",
    "build": "gulp",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
